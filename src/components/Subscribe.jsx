import { useState } from "react";
import axios from "axios";

const Subscribe = () => {
  /**
   * TODO:
   * form state
   * userEmail state (add user auth ???)
   * handleInput
   * handleSubmit
   * fetch to Server (or elsewhere) to send client "Welcome to Supportiveness"
   *  */

  return (
    <div className="subscribe-contianer">
      {/* 
      TODO: 
        Create form here 
        */}
    </div>
  );
};

export default Subscribe;
